<!DOCTYPE html>
<html lang="en">
<head>
    <% include ../components/head %>
</head>
<body>
    <main>
        <% if(json) { %>
            <section>
                <img src="../../../assets/carRoute.png" alt="car route">
                <h1>Start</h1>
                <p>Leave your home and go on a adventure</p>
            </section>
            <a href="<%= nextRoute %>">Step 1</a>
            <ul>
                <li>
                    <a href="/start" class="current">Home</a>
                </li>
                <% json.steps.forEach(function (step, i) { %>
                    <li>
                        <a href="<%= nextRoute %>#<%= i + 1 %>" class="current"><%= i + 1 %></a>
                    </li>
                <% }) %>
                <li>
                    <a href="<%= finishRoute %>">Finish</a>
                </li>
            </ul>
        <% } %>
    </main>
</body>
</html>